# بناء تطبيق Flutter للهاتف

## 📱 بناء APK للهاتف

### الخطوة 1: التحقق من Flutter
```bash
cd flutter-worker-app
flutter doctor
```

### الخطوة 2: بناء APK
```bash
flutter build apk --release
```

الملف سيكون في: `build/app/outputs/flutter-apk/app-release.apk`

### الخطوة 3: تثبيت على الهاتف

#### الطريقة 1: USB
1. فعّل "USB Debugging" في إعدادات المطور على الهاتف
2. وصّل الهاتف بالكمبيوتر
3. شغّل:
```bash
flutter install
```

#### الطريقة 2: نقل الملف يدوياً
1. انسخ ملف `app-release.apk` إلى الهاتف
2. افتح الملف على الهاتف
3. اسمح بتثبيت التطبيقات من مصادر غير معروفة (إذا طُلب)
4. اضغط "تثبيت"

## 🔧 إعدادات Android

### تفعيل USB Debugging

1. اذهب إلى **الإعدادات** > **حول الهاتف**
2. اضغط على **رقم الإصدار** 7 مرات
3. ارجع إلى **الإعدادات** > **خيارات المطور**
4. فعّل **USB Debugging**

### السماح بتثبيت APK

1. اذهب إلى **الإعدادات** > **الأمان**
2. فعّل **مصادر غير معروفة** أو **تثبيت تطبيقات غير معروفة**

## 📦 بناء App Bundle (لـ Google Play)

```bash
flutter build appbundle --release
```

الملف سيكون في: `build/app/outputs/bundle/release/app-release.aab`

## 🍎 بناء iOS (يتطلب Mac)

```bash
flutter build ios --release
```

## ⚙️ إعدادات إضافية

### تغيير اسم التطبيق

في `android/app/src/main/AndroidManifest.xml`:
```xml
android:label="Mirmaia Worker"
```

### تغيير Package Name

في `android/app/build.gradle`:
```gradle
applicationId "com.mirmaia.worker"
```

### تغيير الأيقونة

ضع الأيقونة في:
- `android/app/src/main/res/mipmap-*/ic_launcher.png`

## 🚀 اختبار على الهاتف

### الطريقة 1: USB Debugging
```bash
flutter run --release
```

### الطريقة 2: APK
1. ابني APK
2. انسخه للهاتف
3. ثبّته

## 📝 ملاحظات مهمة

1. **الإنترنت**: التطبيق يحتاج اتصال بالإنترنت للعمل
2. **Firebase**: تأكد من أن Firebase Rules تسمح بالوصول
3. **الصلاحيات**: التطبيق يحتاج صلاحية الإنترنت فقط

## 🔍 استكشاف الأخطاء

### خطأ: "Gradle build failed"
```bash
cd android
./gradlew clean
cd ..
flutter clean
flutter pub get
flutter build apk --release
```

### خطأ: "SDK not found"
- تأكد من تثبيت Android SDK
- شغّل `flutter doctor` للتحقق

### خطأ: "Signing config not found"
- للاختبار، استخدم `--debug` بدلاً من `--release`
- أو أضف signing config في `build.gradle`

---

**جاهز للاستخدام على الهاتف! 📱**
