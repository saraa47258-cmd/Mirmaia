# دليل الصلاحيات - تطبيق Mirmaia Worker

## 📋 جميع الصفحات المتاحة

التطبيق يدعم الصفحات التالية حسب الصلاحيات:

### 1. منيو الموظفين (staff-menu)
- **الصلاحية:** `staff-menu` أو `menu`
- **الوصف:** عرض المنتجات والتصنيفات
- **الميزات:**
  - عرض جميع المنتجات
  - تصفية حسب التصنيف
  - عرض الصور والأسعار

### 2. الطلبات (orders)
- **الصلاحية:** `orders`
- **الوصف:** عرض وإدارة الطلبات
- **الميزات:**
  - عرض جميع الطلبات في الوقت الفعلي
  - عرض تفاصيل الطلب
  - تحديث حالة الطلب (قيد الانتظار → قيد التحضير → جاهز)

### 3. الطاولات (tables)
- **الصلاحية:** `tables`
- **الوصف:** عرض حالة الطاولات
- **الميزات:**
  - عرض جميع الطاولات
  - عرض حالة كل طاولة (متاحة/مشغولة/محجوزة)
  - عرض السعة

### 4. الغرف (rooms)
- **الصلاحية:** `rooms`
- **الوصف:** عرض حالة الغرف
- **الميزات:**
  - عرض جميع الغرف
  - عرض حالة كل غرفة
  - عرض السعة

### 5. الكاشير (cashier)
- **الصلاحية:** `cashier`
- **الوصف:** نظام نقاط البيع
- **الميزات:**
  - عرض المنتجات
  - إضافة منتجات للسلة
  - إنشاء طلبات جديدة
  - إدارة السلة

### 6. المخزون (inventory)
- **الصلاحية:** `inventory`
- **الوصف:** إدارة المخزون
- **الميزات:**
  - عرض عناصر المخزون (قريباً)
  - إدارة الكميات (قريباً)

### 7. التقارير (reports)
- **الصلاحية:** `reports`
- **الوصف:** عرض التقارير
- **الميزات:**
  - تقارير المبيعات (قريباً)
  - تقارير يومية (قريباً)

### 8. المنتجات (products)
- **الصلاحية:** `products`
- **الوصف:** إدارة المنتجات
- **الميزات:**
  - عرض جميع المنتجات
  - تصفية حسب التصنيف
  - عرض التفاصيل

## 🔐 نظام الصلاحيات

### أنواع الصلاحيات

#### 1. الصلاحيات البسيطة (Array)
```json
{
  "permissions": ["staff-menu", "orders", "tables"]
}
```

#### 2. الصلاحيات الكاملة
```json
{
  "permissions": "full"
}
```

#### 3. الصلاحيات التفصيلية (Detailed)
```json
{
  "detailedPermissions": {
    "modules": {
      "staffMenu": true,
      "orders": true,
      "tables": false,
      "rooms": false,
      "cashier": true,
      "inventory": false,
      "reports": false,
      "products": false
    }
  }
}
```

### الأدوار (Roles)

#### Admin (مدير)
- ✅ جميع الصلاحيات تلقائياً
- ✅ الوصول الكامل لجميع الصفحات

#### Cashier (كاشير)
- ✅ منيو الموظفين
- ✅ الكاشير
- ✅ الطلبات
- ✅ الطاولات
- ✅ الغرف

#### Staff (موظف)
- ✅ منيو الموظفين
- ✅ الطلبات (عرض فقط)

## 📝 كيفية إعطاء الصلاحيات

### من لوحة التحكم:

1. اذهب إلى `/admin/workers`
2. اختر الموظف
3. اضغط "تعديل الصلاحيات"
4. حدد الصلاحيات المطلوبة
5. احفظ

### الصلاحيات المدعومة:

- `staff-menu` - منيو الموظفين
- `orders` - الطلبات
- `tables` - الطاولات
- `rooms` - الغرف
- `cashier` - الكاشير
- `inventory` - المخزون
- `reports` - التقارير
- `products` - المنتجات

## 🎯 أمثلة على الصلاحيات

### موظف خدمة
```json
{
  "permissions": ["staff-menu", "orders"]
}
```

### كاشير
```json
{
  "permissions": ["staff-menu", "cashier", "orders", "tables", "rooms"]
}
```

### مدير مخزون
```json
{
  "permissions": ["staff-menu", "inventory", "products"]
}
```

### مدير
```json
{
  "role": "admin"
}
```
أو
```json
{
  "permissions": "full"
}
```

## ✅ التحقق من الصلاحيات

التطبيق يتحقق تلقائياً من الصلاحيات ويعرض فقط الصفحات المتاحة.

---

**ملاحظة:** الصلاحيات يتم التحقق منها من قاعدة البيانات في كل مرة.
