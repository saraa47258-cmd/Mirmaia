# دليل البدء السريع - تطبيق Mirmaia Worker

## ✅ التحقق من المتطلبات

قبل البدء، تأكد من:

1. ✅ **Flutter مثبت**
   ```bash
   flutter --version
   ```
   يجب أن يكون الإصدار 3.0.0 أو أحدث

2. ✅ **Android Studio أو VS Code مثبت**
   - Android Studio: https://developer.android.com/studio
   - VS Code: https://code.visualstudio.com/

3. ✅ **محاكي Android أو جهاز حقيقي**
   - Android Emulator (من Android Studio)
   - أو جهاز Android حقيقي مع USB Debugging مفعّل

## 🚀 خطوات التشغيل

### الخطوة 1: الانتقال إلى المجلد
```bash
cd flutter-worker-app
```

### الخطوة 2: تثبيت التبعيات
```bash
flutter pub get
```

### الخطوة 3: التحقق من الأجهزة المتاحة
```bash
flutter devices
```

يجب أن ترى جهازاً واحداً على الأقل (محاكي أو جهاز حقيقي).

### الخطوة 4: تشغيل التطبيق
```bash
flutter run
```

## 📱 اختبار التطبيق

### 1. إنشاء حساب عامل للاختبار

1. افتح لوحة التحكم: `http://localhost:3000/admin/workers`
2. اضغط "إضافة عامل جديد"
3. أدخل البيانات:
   - الاسم الكامل: "عامل تجريبي"
   - اسم المستخدم: "worker1"
   - كلمة المرور: "1234"
   - الصلاحيات: اختر على الأقل "staff-menu"
4. احفظ

### 2. تسجيل الدخول في التطبيق

1. شغّل التطبيق
2. أدخل:
   - اسم المستخدم: `worker1`
   - كلمة المرور: `1234`
3. اضغط "تسجيل الدخول"

## 🔧 استكشاف الأخطاء

### خطأ: "No devices found"
**الحل:**
```bash
# افتح Android Studio
# Tools > Device Manager
# اضغط Play على أي محاكي
```

### خطأ: "Firebase not initialized"
**الحل:**
- تأكد من أن Firebase project موجود
- تحقق من إعدادات Firebase في `lib/main.dart`

### خطأ: "Permission denied"
**الحل:**
1. افتح Firebase Console
2. Realtime Database > Rules
3. تأكد من أن القواعد تسمح بالقراءة والكتابة

### خطأ: "Package not found"
**الحل:**
```bash
flutter clean
flutter pub get
```

### خطأ: "Gradle build failed" (Android)
**الحل:**
```bash
cd android
./gradlew clean
cd ..
flutter clean
flutter pub get
flutter run
```

## 📦 البناء للإنتاج

### Android APK
```bash
flutter build apk --release
```
الملف سيكون في: `build/app/outputs/flutter-apk/app-release.apk`

### Android App Bundle (لـ Google Play)
```bash
flutter build appbundle --release
```
الملف سيكون في: `build/app/outputs/bundle/release/app-release.aab`

## 🎯 الميزات المتاحة

- ✅ تسجيل دخول للعمال
- ✅ عرض الصلاحيات حسب المستخدم
- ✅ منيو الموظفين (staff-menu)
- ✅ عرض الطلبات في الوقت الفعلي (orders)
- ✅ عرض الطاولات (tables)
- ✅ عرض الغرف (rooms)
- ✅ شاشة الكاشير (cashier) - جاهزة للتطوير

## 📝 ملاحظات مهمة

1. **الصلاحيات**: التطبيق يعرض فقط الأقسام التي لديك صلاحية للوصول إليها
2. **الاتصال**: التطبيق يحتاج اتصال بالإنترنت للعمل
3. **Firebase**: يستخدم نفس قاعدة البيانات المستخدمة في لوحة التحكم

## 🆘 الحصول على المساعدة

إذا واجهت أي مشاكل:
1. تحقق من `FLUTTER_SETUP.md` للتفاصيل
2. تحقق من `README.md` للدليل الكامل
3. تأكد من أن Flutter محدث: `flutter upgrade`

---

**جاهز للاستخدام! 🎉**
