# تطبيق Mirmaia Worker - تطبيق العمال

تطبيق Flutter للعمال يعمل حسب الصلاحيات الممنوحة من قبل الأدمن.

## المميزات

- ✅ تسجيل دخول للعمال
- ✅ نظام صلاحيات ديناميكي
- ✅ واجهات مختلفة حسب الصلاحيات:
  - منيو الموظفين (staff-menu)
  - الطلبات (orders)
  - الطاولات (tables)
  - الغرف (rooms)
  - الكاشير (cashier)
- ✅ عرض المنتجات والتصنيفات
- ✅ عرض الطلبات في الوقت الفعلي
- ✅ واجهة مستخدم عربية جميلة

## المتطلبات

- Flutter SDK 3.0.0 أو أحدث
- Dart SDK 3.0.0 أو أحدث
- Firebase project (mirmaia-33acc)

## التثبيت

1. تأكد من تثبيت Flutter:
```bash
flutter --version
```

2. انتقل إلى مجلد المشروع:
```bash
cd flutter-worker-app
```

3. احصل على التبعيات:
```bash
flutter pub get
```

## التشغيل

### Android (محاكي أو جهاز)
```bash
flutter run
```

### iOS (يتطلب Mac)
```bash
flutter run
```

### Web
```bash
flutter run -d chrome
```

## 📱 بناء للهاتف

### بناء APK
```bash
flutter build apk --release
```
الملف: `build/app/outputs/flutter-apk/app-release.apk`

### تثبيت على الهاتف

#### عبر USB:
```bash
flutter install
```

#### يدوياً:
1. انسخ APK للهاتف
2. افتح الملف
3. اضغط "تثبيت"

**للمزيد:** اقرأ `BUILD_FOR_PHONE.md` أو `QUICK_BUILD.md`

## إعداد Firebase

التطبيق مُعد مسبقاً للاتصال بـ Firebase project:
- Project ID: `mirmaia-33acc`
- Database URL: `https://mirmaia-33acc-default-rtdb.firebaseio.com`

## الصلاحيات المدعومة

التطبيق يدعم الصلاحيات التالية:
- `staff-menu` - منيو الموظفين
- `orders` - الطلبات
- `tables` - الطاولات
- `rooms` - الغرف
- `cashier` - الكاشير
- `inventory` - المخزون (قريباً)
- `reports` - التقارير (قريباً)
- `products` - المنتجات (قريباً)

## هيكل المشروع

```
lib/
├── main.dart                 # نقطة البداية
├── providers/
│   └── auth_provider.dart    # إدارة حالة المصادقة
├── services/
│   ├── auth_service.dart     # خدمة المصادقة
│   └── firebase_service.dart # خدمة Firebase
└── screens/
    ├── login_screen.dart      # شاشة تسجيل الدخول
    ├── home_screen.dart       # الشاشة الرئيسية
    ├── staff_menu_screen.dart # منيو الموظفين
    ├── orders_screen.dart     # الطلبات
    ├── tables_screen.dart     # الطاولات
    ├── rooms_screen.dart      # الغرف
    └── cashier_screen.dart    # الكاشير
```

## الاستخدام

1. شغّل التطبيق
2. سجّل الدخول باسم المستخدم وكلمة المرور
3. سيتم عرض الأقسام المتاحة حسب صلاحياتك
4. استخدم القائمة السفلية للتنقل بين الأقسام

## ملاحظات

- التطبيق يعمل مع نفس قاعدة البيانات المستخدمة في لوحة التحكم
- الصلاحيات يتم التحقق منها من قاعدة البيانات
- التطبيق يعرض فقط الأقسام التي لديك صلاحية للوصول إليها

## التطوير المستقبلي

- [ ] إضافة إمكانية إنشاء طلبات جديدة
- [ ] إضافة إمكانية تحديث حالة الطلبات
- [ ] إضافة شاشة الكاشير الكاملة
- [ ] إضافة إشعارات للطلبات الجديدة
- [ ] إضافة دعم Offline mode

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---

**تم التطوير بواسطة:** Auto (Cursor AI)
**التاريخ:** 2026-01-23
