# قهوة الشام - لوحة التحكم (Next.js)

لوحة تحكم احترافية مبنيّة بـ Next.js + TypeScript + Tailwind CSS لإدارة نظام قهوة الشام.

## 🚀 البدء السريع

### التثبيت

```bash
npm install
```

### التطوير

```bash
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

### البناء للإنتاج

```bash
npm run build
npm start
```

## 📁 البنية

```
admin-next/
├── app/
│   ├── login/          # صفحة تسجيل الدخول
│   ├── admin/          # لوحة التحكم (محمية)
│   │   ├── page.tsx    # لوحة التحكم الرئيسية
│   │   ├── orders/     # صفحة الطلبات
│   │   ├── products/   # صفحة المنتجات
│   │   ├── workers/    # صفحة العمال
│   │   └── reports/    # صفحة التقارير
│   └── layout.tsx      # Layout الرئيسي
├── lib/
│   ├── firebase/       # إعدادات Firebase
│   ├── auth.ts         # نظام المصادقة
│   └── components/     # المكونات المشتركة
└── middleware.ts       # Middleware للحماية
```

## 🔐 المصادقة

- **المسار**: `/login`
- **بيانات الدخول الافتراضية**:
  - اسم المستخدم: `admin`
  - كلمة المرور: `admin123`

## 🛡️ حماية الأدوار

جميع صفحات `/admin/*` محمية وتتطلب:
- تسجيل دخول صحيح
- دور `admin` فقط

## 🔥 Firebase

المشروع يستخدم Firebase Realtime Database مع نفس البنية الموجودة في المشروع الأصلي:
- Restaurant ID: `sham-coffee-1`
- Database Path: `restaurant-system/{collection}/{restaurantId}`

## 📝 الميزات

- ✅ لوحة تحكم شاملة
- ✅ إدارة الطلبات مع فلترة وبحث
- ✅ إدارة المنتجات والتصنيفات
- ✅ إدارة العمال
- ✅ تقارير وإحصائيات مفصلة
- ✅ تصميم متجاوب
- ✅ واجهة عربية كاملة

## 🛠️ التقنيات

- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Firebase** (Realtime Database, Auth, Storage)
